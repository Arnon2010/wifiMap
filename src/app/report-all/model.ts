export interface TotalDataAll {
    total_up: string,
    total_down: string,
    total_device: string,
  }

export interface DataTableAll {
    univ_id: string,
    univ_name: string,
    univ_local: string,
    upCount: number,
    downCount: number,
    deviceCount: number,
  }